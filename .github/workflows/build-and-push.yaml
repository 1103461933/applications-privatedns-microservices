name: Build and Push Docker Image

on:
  push:
    branches:
      - main
    paths:
      - 'services/**'
      - '!services/**/helm/**' # EVITA EL BUCLE INFINITO
  workflow_dispatch:
    inputs:
      service:
        description: 'Service to build'
        required: true
        type: choice
        options:
          - booking-service
          - payment-service
          - customer-service
          - all
      version:
        description: 'Version tag (e.g., 1.0.0)'
        required: true
        type: string
        default: '1.0.0'

env:
  DOCKER_USERNAME: ${{ secrets.DOCKER_USERNAME }}
  DOCKER_PASSWORD: ${{ secrets.DOCKER_PASSWORD }}

jobs:
  # JOB 1: DETERMINAR QUÉ SERVICIOS CONSTRUIR
  setup:
    runs-on: ubuntu-latest
    outputs:
      services: ${{ steps.set-services.outputs.services }}
      version: ${{ steps.set-version.outputs.version }}
    steps:
      - id: set-services
        run: |
          if [ "${{ github.event_name }}" == "push" ] || [ "${{ github.event.inputs.service }}" == "all" ]; then
            echo 'services=["booking-service", "payment-service", "customer-service"]' >> $GITHUB_OUTPUT
          else
            echo 'services=["${{ github.event.inputs.service }}"]' >> $GITHUB_OUTPUT
          fi
      
      - id: set-version
        run: |
          if [ "${{ github.event_name }}" == "workflow_dispatch" ]; then
            echo "version=${{ github.event.inputs.version }}" >> $GITHUB_OUTPUT
          else
            echo "version=${GITHUB_SHA::7}" >> $GITHUB_OUTPUT
          fi

  # JOB 2: CONSTRUCCIÓN (EN PARALELO SEGÚN LA MATRIX)
  build-and-push:
    needs: setup
    runs-on: ubuntu-latest
    strategy:
      matrix:
        service: ${{ fromJSON(needs.setup.outputs.services) }}
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v2

      - name: Login to Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{ env.DOCKER_USERNAME }}
          password: ${{ env.DOCKER_PASSWORD }}

      - name: Build and push
        uses: docker/build-push-action@v4
        with:
          context: services/${{ matrix.service }}
          push: true
          tags: |
            ${{ env.DOCKER_USERNAME }}/${{ matrix.service }}:${{ needs.setup.outputs.version }}
            ${{ env.DOCKER_USERNAME }}/${{ matrix.service }}:latest

  # JOB 3: ACTUALIZAR HELM (UN SOLO COMMIT AL FINAL PARA EVITAR CONFLICTOS)
  update-helm:
    needs: [setup, build-and-push]
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3
        with:
          token: ${{ secrets.PAT_TOKEN }}

      - name: Update Helm tags
        run: |
          VERSION="${{ needs.setup.outputs.version }}"
          # Convertimos el string JSON de servicios en un array de bash
          SERVICES_JSON='${{ needs.setup.outputs.services }}'
          SERVICES=$(echo $SERVICES_JSON | jq -r '.[]')
          
          for SERVICE in $SERVICES; do
            VALUES_FILE="services/${SERVICE}/helm/values-dev.yaml"
            if [ -f "$VALUES_FILE" ]; then
              sed -i "s/tag: .*/tag: \"${VERSION}\"/" $VALUES_FILE
              echo "✅ Updated ${SERVICE} to ${VERSION}"
            fi
          done

      - name: Commit and push changes
        run: |
          git config user.name "GitHub Actions Bot"
          git config user.email "actions@github.com"
          
          git add services/*/helm/values-dev.yaml
          git commit -m "chore: update images to version ${{ needs.setup.outputs.version }} [skip ci]" || echo "No changes to commit"
          
          git pull --rebase origin main
          git push origin main