apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: booking-dev
  namespace: argocd
  labels:
    environment: dev
    service: booking
    team: platform
spec:
  project: default
  source:
    repoURL: https://github.com/1103461933/applications-privatedns-microservices.git
    targetRevision: dev
    path: services/booking-service/helm
    helm:
      valueFiles:
        - values-dev.yaml
  destination:
    server: https://kubernetes.default.svc
    namespace: apps-dev
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
      - ApplyOutOfSyncOnly=true
    retry:
      limit: 3
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 60s