replicaCount: 1

image:
  repository: 1103461933/customer-service
  tag: "1.0.13"
  pullPolicy: Always

ingress:
  enabled: true
  className: nginx
  hosts:
    - host: customer.dev.company.internal
      paths:
        - path: /
          pathType: Prefix

resources:
  requests:
    cpu: 50m
    memory: 64Mi
  limits:
    cpu: 100m
    memory: 128Mi

autoscaling:
  enabled: true
  minReplicas: 1
  maxReplicas: 3

configMap:
  data:
    NODE_ENV: "development"
    LOG_LEVEL: "debug"
    SERVICE_NAME: "customer-service"
    SERVICE_VERSION: "1.0.0"